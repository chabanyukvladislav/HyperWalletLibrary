using HyperWalletLibrary.Model;

namespace HyperWalletLibrary.Components
{
    public static class JsonToUserConverter
    {
        public static User Convert(string json)
        {
            User user = new User();
            return user;
        }
    }
}
